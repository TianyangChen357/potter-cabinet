services:
  potter-cabinet:
    build: .
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      APP_PASSWORD: ${APP_PASSWORD:-}
